@page
@model ApTutor.ContentAdmin.Pages.GeneratingLearnModel
@{
    ViewData["Title"] = "Generating learn content";
    if (Model.IsRunning)
    {
        <meta http-equiv="refresh" content="2" />
    }
}

<p><a href="/Review?course=@Model.CourseId&node=@Model.NodeId&difficulty=@Model.Difficulty">&larr; Back to topic</a></p>

@if (Model.IsRunning)
{
    <div class="card">
        <p class="spinner-text">Generating learn content for this topic&hellip; this page will refresh on its own.</p>
    </div>
}
else
{
    <div class="card">
        <p class="error">Generation failed: @Model.Error</p>
        <p><a href="/Review?course=@Model.CourseId&node=@Model.NodeId&difficulty=@Model.Difficulty">Go back and try again</a></p>
    </div>
}
